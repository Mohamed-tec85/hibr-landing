# 📧 دليل تفعيل الإيميل التلقائي

موقعك الآن مُجهَّز ليرسل إيميل تأكيد طلب احترافي مع روابط التحميل تلقائيًا. هذا الدليل يشرح كيف تفعّل هذي الميزة في 20 دقيقة.

---

## 📋 ما ستحصل عليه

عند إتمام أي طلب، المشتري سيستلم إيميل احترافي يحتوي:
- ✅ تصميم متناسق مع هوية الموقع (ذهبي + بنفسجي + خلفية داكنة)
- ✅ رقم الطلب وملخّص كامل
- ✅ زر **"⬇ تحميل الملف"** لكل منتج مع رابط مباشر آمن
- ✅ الروابط تعمل **لمدة 7 أيام كاملة** (محمية بـ Signed URLs)
- ✅ ملخّص مالي (المجموع، الضريبة، الإجمالي)
- ✅ يدعم العربية والإنجليزية تلقائيًا حسب اختيار المشتري

---

## 🎯 المتطلّبات

ستحتاج **حسابين مجانيين** فقط:

1. **[Resend](https://resend.com)** — لإرسال الإيميلات (3,000 إيميل/شهر مجانًا)
2. **Supabase** — موجود عندك بالفعل، لكن نحتاج مفتاحًا إضافيًا

---

## 🚀 الخطوات بالتفصيل

### الخطوة 1: إنشاء حساب Resend

1. روح إلى **[resend.com](https://resend.com)**
2. اضغط **"Sign Up"** (مجاني، بدون بطاقة)
3. سجّل بإيميلك (يفضّل نفس إيميل عملك)
4. أكّد البريد من رسالة التفعيل

### الخطوة 2: التحقّق من نطاقك (Domain)

⚠️ **هذا الجزء مهم جدًا** — بدون التحقّق، الإيميلات ستذهب للـ Spam.

1. في لوحة Resend اضغط **"Domains"** من القائمة الجانبية
2. اضغط **"Add Domain"**
3. أدخل: `chattlle.com`
4. اضغط **"Add"**
5. Resend ستعطيك **سجلات DNS** يجب إضافتها (TXT, MX, CNAME)
6. روح إلى **Hostinger** → إدارة الدومين `chattlle.com` → **DNS / Nameservers**
7. أضف **كل سجل** أعطته إياك Resend بالضبط
8. ارجع لـ Resend وانتظر 5-15 دقيقة، ثم اضغط **"Verify"**
9. لازم تظهر علامة ✅ خضراء على كل السجلات

> 💡 **إذا واجهت صعوبة في التحقّق:** يمكنك مؤقتًا استخدام النطاق الافتراضي `onboarding@resend.dev` لكن سيكون في حدود 100 إيميل/يوم. سأشرح هذا الخيار في النهاية.

### الخطوة 3: إنشاء API Key في Resend

1. من القائمة الجانبية اضغط **"API Keys"**
2. اضغط **"+ Create API Key"**
3. الاسم: `Chattlle Production`
4. الصلاحية: **Sending access** (أو Full access)
5. اضغط **"Add"**
6. **انسخ المفتاح فورًا** (يبدأ بـ `re_...`) — لن يظهر مرة أخرى!
7. احتفظ به في مكان آمن مؤقتًا

### الخطوة 4: الحصول على Supabase Service Key

نحتاج هذا المفتاح لإنشاء روابط التحميل الآمنة من جهة الخادم.

⚠️ **مفتاح حسّاس جدًا — لا تشاركه أبدًا في الكود أو على الإنترنت!**

1. ادخل [supabase.com/dashboard](https://supabase.com/dashboard) → مشروعك `chattlle`
2. ⚙️ Settings → **API Keys** (أو Settings → API)
3. ستجد قسم **"Secret keys"**
4. اضغط أيقونة العين 👁️ بجانب `default` ثم انسخه (يبدأ بـ `sb_secret_...` أو `eyJhbGci...`)
5. احتفظ به مع مفتاح Resend

### الخطوة 5: رفع الكود الجديد على Netlify

1. ادخل **Netlify** → موقعك → **Deploys**
2. اسحب مجلد `chattlle` الجديد (الموجود في `chattlle-v6-search-email.zip`)
3. انتظر اكتمال النشر

> 💡 الملف الجديد يحتوي على:
> - `netlify.toml` — يفعّل الـ Functions
> - `netlify/functions/send-order-email.js` — منطق إرسال الإيميل

### الخطوة 6: إضافة المتغيّرات السرّية في Netlify

هذي الخطوة هي اللي تربط الأسرار بالـ Function:

1. في Netlify → موقعك → **Site configuration**
2. اضغط **"Environment variables"** من القائمة الجانبية
3. اضغط **"Add a variable"**
4. أضف هذي المتغيّرات الأربعة، **واحدًا واحدًا**:

| Key | Value |
|-----|-------|
| `RESEND_API_KEY` | المفتاح من Resend (يبدأ بـ `re_`) |
| `SUPABASE_URL` | `https://opwqklcttccnixvzspdx.supabase.co` |
| `SUPABASE_SERVICE_KEY` | المفتاح السرّي من Supabase |
| `FROM_EMAIL` | `orders@chattlle.com` (أو أي بريد تختاره مع نطاقك) |

5. بعد الإضافة، اضغط **"Save"**

### الخطوة 7: إعادة النشر لتفعيل المتغيّرات

المتغيّرات الجديدة لن تعمل إلا بعد إعادة نشر:

1. في Netlify → **Deploys**
2. اضغط على آخر deploy → **"Trigger deploy"** → **"Deploy site"**
3. انتظر اكتمال النشر

### الخطوة 8: اختبر!

1. ادخل موقعك واشترِ منتجًا تجريبيًا
2. أكمل عملية الدفع بإيميلك
3. بعد بضع ثوانٍ، افتح بريدك (وفولدر **Junk/Spam** أيضًا)
4. لازم يصلك إيميل بتصميم Chattlle الذهبي مع زر التحميل ✨

---

## ✅ التحقّق من النجاح

في Netlify يمكنك مراقبة الـ Function:

1. **Netlify** → موقعك → **Functions** → `send-order-email`
2. ستجد سجل لكل استدعاء (نجاح/فشل)
3. لو فشل، اضغط على السطر لرؤية رسالة الخطأ

في Resend يمكنك مراقبة الإيميلات المرسلة:

1. **Resend Dashboard** → **Emails**
2. ستجد سجل بكل إيميل، حالة الإرسال، وإذا فُتح

---

## 🆘 المشاكل الشائعة وحلولها

| المشكلة | السبب | الحل |
|---------|--------|------|
| الإيميل لا يصل | لم يتم التحقّق من النطاق | راجع الخطوة 2 |
| الإيميل في Spam | السجلات DNS غير مكتملة | تأكد من إضافة كل سجلات DKIM و SPF |
| `Email send failed` في Netlify Function | مفتاح Resend خاطئ | تأكد من المفتاح في Environment variables |
| الزر "تحميل" لا يعمل في الإيميل | مفتاح Supabase Service خاطئ | تأكد من نسخ المفتاح الصحيح |
| `Function not available` | لم يتم رفع المجلد netlify | تأكد من رفع المجلد كاملاً |

---

## 🆓 خيار بديل: استخدام نطاق Resend الافتراضي

إذا لم تستطع التحقّق من نطاق `chattlle.com` الآن، استخدم النطاق الافتراضي مؤقتًا:

1. في الخطوة 6 (Environment variables) ضع:
   - **FROM_EMAIL:** `onboarding@resend.dev`
2. الإيميلات ستعمل لكن:
   - 💡 المرسل سيظهر باسم `onboarding@resend.dev` بدلاً من `orders@chattlle.com`
   - 💡 الحد الأقصى **100 إيميل/يوم**
   - 💡 الإيميلات ستصل فقط للإيميل المُسجَّل به حساب Resend (لتجربتها)
3. بعد التحقّق من نطاقك لاحقًا، غيّر الـ FROM_EMAIL وأعد النشر

---

## 💡 ملاحظات احترافية

- **التكلفة المتوقّعة:** مجاني تمامًا حتى 3,000 إيميل/شهر (يكفي لمتجر متوسط)
- **بعد 3,000 إيميل:** $0.001 لكل إيميل إضافي
- **حماية إضافية:** يمكن لاحقًا إضافة CAPTCHA لمنع رسائل وهمية
- **تخصيص الإيميل:** يمكنك تعديل التصميم في `netlify/functions/send-order-email.js` (الدالة `buildEmailHtml`)

---

## 🚀 ماذا بعد؟

موقعك الآن متكامل تقريبًا! الخطوات التالية المقترحة:

1. **💳 تفعيل بوابة الدفع الحقيقية** (Moyasar) — راجع `دليل-تفعيل-الدفع.md`
2. **🎁 نظام أكواد خصم**
3. **⭐ تقييمات من العملاء**
4. **📱 تطبيق جوال PWA**

أي خطوة منها، أنا معك ✦

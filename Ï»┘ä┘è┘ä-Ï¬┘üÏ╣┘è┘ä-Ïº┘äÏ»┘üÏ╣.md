# 💳 دليل تفعيل بوابة الدفع الحقيقية

موقعك حاليًا فيه نظام دفع تجريبي (Demo) — يحاكي تجربة الشراء الكاملة لكن لا يخصم أموالًا حقيقية. هذا الدليل يشرح كيف تفعّل الدفع الحقيقي عندما تكون جاهزًا.

---

## 📋 ما تحتاجه قبل البدء

### 1. سجل تجاري (مطلوب لكل بوابات الدفع)
- 🇸🇦 في السعودية: سجل تجاري ساري من وزارة التجارة
- إن لم يكن لديك سجل تجاري، فكّر في خيارات بديلة:
  - **Gumroad** أو **Lemon Squeezy** (يبيعون نيابةً عنك ويتولّون كل شيء)
  - **Salla** أو **Zid** (منصّات تجارة إلكترونية كاملة)

### 2. حساب في بوابة دفع

#### الخيار الأول: Moyasar (الأنسب للسعودية)
- موقعهم: [moyasar.com](https://moyasar.com)
- العمولة: 2.85% + 1 ريال لكل عملية
- يدعمون: مدى، فيزا، ماستركارد، Apple Pay، STC Pay
- لوحة تحكم عربية ممتازة

#### الخيار الثاني: Tap Payments
- موقعهم: [tap.company](https://www.tap.company)
- يدعمون: نفس وسائل الدفع تقريبًا
- مناسب للسعودية، الإمارات، الكويت، البحرين
- عمولة قريبة من Moyasar

### 3. خادم (Backend) — الجزء الأهم!

**⚠️ ملاحظة حرجة:** بوابات الدفع تتطلّب **مفتاحًا سرّيًا** (Secret Key) لا يجب أبدًا وضعه في كود الموقع (الـ Frontend) لأنه سيكون مرئيًا لأي شخص يفتح أدوات المطوّر في المتصفح.

لذا تحتاج خادمًا يستقبل بيانات الدفع من موقعك، ثم يتواصل بشكل آمن مع بوابة الدفع.

**خيارات الخوادم السهلة:**

| الخدمة | الميزة | السعر |
|--------|--------|--------|
| **Netlify Functions** | مدمجة مع استضافتك الحالية | مجاني حتى 125K طلب/شهر |
| **Vercel Functions** | سريعة وسهلة | مجاني حتى 100K طلب/شهر |
| **Firebase Functions** | من Google | مجاني محدود + دفع حسب الاستخدام |

**التوصية:** استخدم **Netlify Functions** لأن موقعك بالفعل على Netlify.

---

## 🔧 الخطوات العملية (مع Moyasar + Netlify Functions)

### الخطوة 1: سجّل في Moyasar واحصل على المفاتيح
1. ادخل [dashboard.moyasar.com](https://dashboard.moyasar.com) وأنشئ حساب
2. أكمل بيانات السجل التجاري والتحقّق
3. روح إلى **Settings → API Keys**
4. ستجد مفتاحين:
   - **Publishable Key** (يبدأ بـ `pk_test_` أو `pk_live_`) — يمكن وضعه في Frontend
   - **Secret Key** (يبدأ بـ `sk_test_` أو `sk_live_`) — **سرّي تمامًا**!
5. ابدأ بـ Test Keys للتجربة، ثم انتقل لـ Live Keys بعد التأكد

### الخطوة 2: أنشئ Netlify Function
في مجلد موقعك، أنشئ هذا الهيكل:

```
chattlle/
├── netlify/
│   └── functions/
│       └── create-payment.js   ← الكود الجديد
├── netlify.toml                ← إعدادات Netlify
├── index.html
├── checkout.html
└── ...
```

**ملف `netlify.toml`:**
```toml
[build]
  functions = "netlify/functions"
  publish = "."
```

**ملف `netlify/functions/create-payment.js`:**
```javascript
exports.handler = async (event) => {
  // CORS headers
  const headers = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'Content-Type',
    'Access-Control-Allow-Methods': 'POST, OPTIONS'
  };

  if (event.httpMethod === 'OPTIONS') {
    return { statusCode: 200, headers, body: '' };
  }

  if (event.httpMethod !== 'POST') {
    return { statusCode: 405, headers, body: 'Method not allowed' };
  }

  try {
    const data = JSON.parse(event.body);
    const SECRET_KEY = process.env.MOYASAR_SECRET_KEY;

    // Validate
    if (!data.amount || !data.source) {
      return {
        statusCode: 400,
        headers,
        body: JSON.stringify({ error: 'Missing required fields' })
      };
    }

    // Call Moyasar API
    const response = await fetch('https://api.moyasar.com/v1/payments', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Basic ' + Buffer.from(SECRET_KEY + ':').toString('base64')
      },
      body: JSON.stringify({
        amount: data.amount, // في halalas (1 ريال = 100 halala)
        currency: 'SAR',
        description: data.description || 'Chattlle order',
        source: data.source,
        callback_url: data.callback_url,
        metadata: data.metadata
      })
    });

    const result = await response.json();

    return {
      statusCode: response.ok ? 200 : 400,
      headers,
      body: JSON.stringify(result)
    };
  } catch (err) {
    return {
      statusCode: 500,
      headers,
      body: JSON.stringify({ error: err.message })
    };
  }
};
```

### الخطوة 3: أضف المفتاح السرّي في Netlify
1. ادخل لوحة Netlify
2. روح إلى **Site Settings → Environment variables**
3. اضغط **Add a variable**:
   - **Key:** `MOYASAR_SECRET_KEY`
   - **Value:** المفتاح السرّي من Moyasar (يبدأ بـ `sk_test_`)
4. احفظ وأعد نشر الموقع

### الخطوة 4: عدّل ملف `checkout.html`
افتح `checkout.html` وابحث عن هذا التعليق:

```javascript
/* =================================================================
   💳 REAL PAYMENT INTEGRATION POINT
   ================================================================= */

// DEMO: simulate processing delay
setTimeout(() => { ... }, 2000);
```

**استبدل الجزء الذي يبدأ من `// DEMO:` بهذا الكود:**

```javascript
try {
  const cardNumber = document.getElementById('cCardNumber').value.replace(/\s/g, '');
  const expiry = document.getElementById('cExpiry').value;
  const cvc = document.getElementById('cCvc').value;
  const cardName = document.getElementById('cCardName').value;
  const [month, year] = expiry.split('/');

  const response = await fetch('/.netlify/functions/create-payment', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      amount: Math.round(getCartTotal() * 100), // halalas
      description: `Chattlle order — ${getCartCount()} items`,
      source: {
        type: 'creditcard',
        name: cardName,
        number: cardNumber,
        cvc: cvc,
        month: month,
        year: '20' + year
      },
      callback_url: window.location.origin + '/success.html',
      metadata: {
        customer_name: name,
        customer_email: email,
        customer_phone: phone
      }
    })
  });

  const result = await response.json();

  if (result.status === 'paid' || result.status === 'authorized') {
    // ✅ نجح الدفع
    const orderId = result.id;
    saveOrder({
      id: orderId,
      customer: { name, email, phone },
      items: getCartDetailed(),
      subtotal: getCartSubtotal(),
      vat: getCartVAT(),
      total: getCartTotal(),
      method: selectedPaymentMethod,
      date: new Date().toISOString()
    });
    clearCart();
    window.location.href = 'success.html';
  } else if (result.source && result.source.transaction_url) {
    // 🔐 يحتاج 3D Secure — التوجيه لصفحة البنك
    window.location.href = result.source.transaction_url;
  } else {
    // ❌ فشل الدفع
    showToast(result.message || 'فشل الدفع، حاول مرة أخرى', '✕');
    btn.disabled = false;
    btn.innerHTML = `${getText('btn.placeOrder', lang)}`;
  }
} catch (err) {
  showToast('حدث خطأ، حاول مرة أخرى', '✕');
  console.error(err);
  btn.disabled = false;
  btn.innerHTML = `${getText('btn.placeOrder', lang)}`;
}
```

### الخطوة 5: احذف "بانر التجريب"
في `checkout.html` احذف هذا السطر:
```html
<div class="demo-banner">${getText('checkout.demoNotice', lang)}</div>
```

### الخطوة 6: اختبر بالبطاقات التجريبية
Moyasar يقدّم بطاقات اختبار:

| النوع | رقم البطاقة | CVC | الانتهاء |
|-------|-------------|-----|----------|
| نجاح | `4111 1111 1111 1111` | `123` | `12/30` |
| فشل | `4000 0000 0000 0002` | `123` | `12/30` |
| 3D Secure | `4000 0000 0000 3220` | `123` | `12/30` |

### الخطوة 7: انتقل للوضع الحي (Live)
بعد التأكد أن كل شيء يعمل:
1. ارجع لـ Moyasar Dashboard
2. أكمل التحقق من السجل التجاري
3. احصل على **Live Keys**
4. حدّث `MOYASAR_SECRET_KEY` في Netlify بالمفتاح الحي
5. أعد النشر — الآن أنت تستقبل أموالًا حقيقية! 🎉

---

## 🔒 إضافات أمان مهمة

1. **Webhooks**: أنشئ Function آخر لاستقبال إشعارات Moyasar (للتأكد من نجاح الدفع حتى لو أغلق المستخدم المتصفح)
2. **تشفير ملفات التحميل**: لا ترفع ملفاتك مباشرة. استخدم خدمة مثل **AWS S3** مع روابط تنتهي صلاحيتها
3. **قاعدة بيانات**: لحفظ الطلبات بشكل دائم استخدم **Supabase** أو **Firebase** (مجاني للبدء)
4. **إرسال الإيميلات**: استخدم **Resend** أو **SendGrid** لإرسال روابط التحميل تلقائيًا

---

## 🆘 احتمالات بحاجة لمساعدتي فيها

عندما تكون جاهزًا، اطلب مني أيًا من هذي وأكمل معك:

- ✅ ربط Moyasar كامل (الـ Function + التحديثات على checkout.html)
- ✅ إنشاء webhook لتأكيد الدفعات
- ✅ ربط Supabase لحفظ الطلبات والعملاء
- ✅ نظام إرسال إيميل تلقائي بروابط التحميل
- ✅ لوحة تحكم Admin لإدارة المنتجات والطلبات

---

## 📚 مصادر مفيدة

- 📖 توثيق Moyasar الرسمي: [docs.moyasar.com](https://docs.moyasar.com)
- 📖 توثيق Tap: [developers.tap.company](https://developers.tap.company)
- 📖 Netlify Functions: [docs.netlify.com/functions](https://docs.netlify.com/functions/overview/)
- 💬 مجتمع Moyasar للدعم بالعربية

✦ موقعك جاهز للتجارة. الباقي مجرّد تفعيل!
